# config/ — single source of truth for all presentation data that appears on slides.
# Charts, tables, labels and Slide 59 must all obtain values from here.
# To change a result: edit this package and rebuild — never edit slide text manually.
